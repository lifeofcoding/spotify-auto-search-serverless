# spotify-auto-search-serverless

## Setup
Run `yarn`

Configure Spotify application. Place client secret in `secrets-example.ts`. Rename to `secrets.ts`

## Running locally
Run `serverless offline`

## Deploying

* Install AWS CLI
* Configue AWS credentials
* `serverless deploy`